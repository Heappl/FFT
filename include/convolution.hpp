#pragma once

#include <vector>
#include "dft.hpp"

namespace convolution
{

template <typename T>
std::vector<T> convolve(const std::vector<T>& first,
                        const std::vector<T>& second)
{
    return first;
}

} //namespace convolution

